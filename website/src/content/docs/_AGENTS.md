# Docs content lives here

The agent guide for this directory is [`../AGENTS.md`](../AGENTS.md) (one level
up, because the content loader would publish an `AGENTS.md` placed here).

Quick reminders:

- `.mdoc` only; the sidebar is autogenerated from the directory tree.
- Complete QQL goes in `{% qqlExample %}`; raw ```qql fences fail
  `pnpm validate:qql`.
- Run `pnpm check` before claiming a docs change is done.
