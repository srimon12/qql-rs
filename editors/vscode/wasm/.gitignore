# Generated by `wasm-pack build crates/qql-wasm --target bundler --out-dir ../../editors/vscode/wasm`.
# Track only the known bundle files: anything new a wasm-bindgen upgrade starts
# emitting must be added here explicitly, otherwise a rebuild would silently
# drop required glue files from the repo (this exact bug shipped once).
*
!.gitignore
!LICENSE
!README.md
!package.json
!qql_wasm.d.ts
!qql_wasm.js
!qql_wasm_bg.js
!qql_wasm_bg.wasm
!qql_wasm_bg.wasm.d.ts
