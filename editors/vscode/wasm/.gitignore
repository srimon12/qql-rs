# Generated by `wasm-pack build crates/qql-wasm --target nodejs --out-dir ../../editors/vscode/wasm`.
# The extension loads this bundle via plain require() (CJS), so the bundle MUST
# be the nodejs target. A --target bundler rebuild emits an ESM entry plus a
# split glue file (qql_wasm_bg.js) that Node 22 / VS Code cannot load.
# Only the known nodejs-target outputs are whitelisted; anything new must be
# added here explicitly or it will be silently ignored by '*'.
*
!.gitignore
!LICENSE
!README.md
!package.json
!qql_wasm.d.ts
!qql_wasm.js
!qql_wasm_bg.wasm
!qql_wasm_bg.wasm.d.ts
