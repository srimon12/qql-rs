[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "pyqql"
version = "0.1.0"
requires-python = ">=3.8"
description = "QQL parser — Qdrant Query Language native parser via Rust"
readme = "README.md"
license = { text = "MIT" }
