[package]
name = "qql-conformance"
version.workspace = true
edition.workspace = true
publish = false

[dependencies]
qql-core = { path = "../qql-core", features = ["serde", "std"] }
qql-plan = { path = "../qql-plan" }
serde_json = "1"
